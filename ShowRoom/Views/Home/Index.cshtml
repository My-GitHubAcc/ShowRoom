@model IEnumerable<Marque>
@{
    ViewData["Title"] = "Home";
}

<div style="margin:10px">
    @*<input type="text" style="margin:10px;" 
           class="col-md-offset-1 col-md-3 form-control" 
           placeholder="Search for a Marque" 
           onchange=""/>
    <hr />*@
    @foreach (var marque in Model)
    {
    <div class="col-sm-3 col-lg-3 col-md-3">
        <a class="panel panel-primary btn" asp-controller="Car" asp-action="ShowModels" asp-route-id="@marque.MarqueId">
            <div class="panel-heading">
                <h4>@marque.Name</h4>
            </div>
            <div class="panel-body">
                @* An Image placeholder will go here *@

            </div>
        </a>
    </div>
    }
</div>